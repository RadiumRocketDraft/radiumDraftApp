import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  tooltipContainer: {
    flexDirection: 'row',
    alignItems: 'center',
    justifyContent: 'space-between',
  },
  slider: {
    flex: 1,
  },
});
